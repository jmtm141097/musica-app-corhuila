{{> head}}

{{> navbar }}
<div class="row">
    <div class="col-sm-8">
        <div class="row">
            <div class="col-sm-12 contenido">
                <h1>Artistas!</h1>
                {{ prettyJson artistas }}
            </div>
        </div>
    </div>
    {{> buscador}}
</div>
{{> footer}}